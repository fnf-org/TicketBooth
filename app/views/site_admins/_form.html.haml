= form_for(@site_admin) do |f|
  = render 'shared/form_errors', resource: @site_admin

  .field
    = f.label :user_id
    = f.number_field :user_id, class: 'input-small', min: 1, max: User.last.id, required: true

  .actions
    = f.submit 'Add Site Admin', class: 'btn btn-primary'
