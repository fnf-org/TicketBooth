  // window.addEventListener("load", function () {
  //   // Initial all datetimepickers on the page, if there are any
  //
  //   $('.input-datetimepicker').each(function (index, el) {
  //     const {data, datetimepicker} = $(el), date = data('date');
  //
  //     datetimepicker({
  //       format: 'dd/MM/yyyy hh:mm:ss', pick12HourFormat: true, pickSeconds: false, format: 'yyyy/MM/dd hh:mm:ss'
  //     });
  //
  //     // Rails returns a UTC date, so need to convert it to local time
  //     if (date) {
  //       data('datetimepicker').setLocalDate(new Date(date));
  //     }
  //   });
  //
  // });
